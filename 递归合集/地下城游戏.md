```
class Solution {
    public int calculateMinimumHP(int[][] dungeon) {
        ArrayList<int[]> arr = new ArrayList<int[]>();
        
        int res=Integer.MIN_VALUE;
        int min=Integer.MAX_VALUE;

        res = dfs(dungeon,0,0,min,0,res);
        return res;

        
    }
    public int dfs(int[][] dungeon,int i,int j,int min,int sum,int res){
        sum+=dungeon[i][j];
        min = Math.min(min,sum);
        System.out.println("i="+i+",j="+j+",min="+min+",sum="+sum+",res="+res);

        if(i==dungeon.length-1 && j == dungeon[0].length-1){
        
        res = Math.max(res,min);
        System.out.println(res);
        min =0;
        return res;
        }
        
        if(i<=dungeon.length-2 && j<=dungeon[0].length-1){
            dfs(dungeon,i+1,j,min,sum,res);
        }
     
         if(i<=dungeon.length-1 && j<=dungeon[0].length-2){
            dfs(dungeon,i,j+1,min,sum,res);
        }
        return res;
    }
}
```
